# MAPA — squad-infra

**Propósito:** Infraestrutura interna — segurança, hardening, hooks bloqueantes, auditoria do squad.

## Status

⚪ Em construção. Atualmente sem agentes próprios — operação executada por `@devops` (squad-dev) e `@analista-qa` (squad-dev).

## Estrutura prevista

`agentes/` (criar quando primeiro agente dedicado nascer)
